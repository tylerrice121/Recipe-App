<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Recipe</title>
</head>
<body>
        <header>
            <nav>
                <a href="/new">add recipe</a>
                <a href="/dashboard">Dashboard</a>
                <a href="/logout">Logout</a>
            </nav>
        </header>
        <div class="container">
            <div class="left-side">
                <h1><%= r.title %></h1>
                <img src="/<%= r.img %>" style="width: 350px;">
                <h1>IMG</h1>
                <h4>DESCRIPTION</h4>
                <p><%= r.description %></p>
                <p>PREPTIME
                    <p><%= r.prepTime %> min</p>
                </p>
                <p>COOKTIME
                    <p><%= r.cookTime %> min</p>
                </p>
            </div>
            <div class="right-side">
                <h3>INGREDIENTS</h3>
                <p><%= r.ingredients %> </p>
                <h3>STEPS</h3>
                <p><%= r.steps %></p>
            </div>
            <%if (user == r.user) {%>
            <%console.log(user)%>
            <a href="/recipe/<%=r._id%>/edit">EDIT RECIPE</a>
            <form action="/recipe/<%=r._id%>?_method=DELETE" method="POST">
                <input type="submit"value="DELETE RECIPE">
            </form>
            <%}%>
        </div>
</body>
</html>